[user]
    email = loicfontaine@fastmail.fm
    name  = Loïc Fontaine

[core]
    editor       = emacsclient -t -a nano
    excludesfile = ~/.gitignore

[diff]
    tool = ediff
[difftool "ediff"]
    cmd    = emacs -n -diff $LOCAL $REMOTE
    prompt = false

[merge]
    tool = ediff
[mergetool "ediff"]
    cmd        = emacs -n -merge $LOCAL $REMOTE $BASE $MERGED
    keepBackup = false
    prompt     = false

#[url "https://"]
#  insteadOf = git://

[color]
    ui = true

[push]
    default = simple
